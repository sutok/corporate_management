# 権限システム統合完了報告

**作成日**: 2026-01-03
**作業内容**: 全APIルーターへの権限システム統合
**実施時間**: 約3時間

---

## 📊 実装サマリー

### 完了したルーター (6/6)

| ルーター | 実装パターン | 特徴 | コミットハッシュ |
|---------|------------|------|----------------|
| **branches.py** | Pattern 1: Basic CRUD | シンプルなCRUD権限 | 45f49d8 |
| **departments.py** | Pattern 1: Basic CRUD | シンプルなCRUD権限 | 4f4d789 |
| **companies.py** | Pattern 1: Basic CRUD | 新規権限2件追加 | a2a92be |
| **users.py** | Pattern 2: 自己操作許可 | update_self対応 | 17d3188 |
| **customers.py** | Pattern 2: 自己操作許可 | view_assigned対応準備 | 3abca48 |
| **daily_reports.py** | Pattern 3: 動的スコープ | view_all/view_self切替 | 2b47882 |

### 追加された権限 (2件)

| 権限コード | 名前 | 説明 |
|-----------|------|------|
| `company.create` | 企業作成 | 新規企業を作成する権限（システム管理者専用） |
| `company.delete` | 企業削除 | 企業を削除する権限（システム管理者専用、通常使用しない） |

---

## 🎯 実装パターン詳細

### Pattern 1: Basic CRUD

**適用ルーター**: branches.py, departments.py, companies.py

**実装方法**:
```python
from app.auth.permissions import require_permission

@router.get("")
async def list_resources(
    current_user: User = Depends(require_permission("resource.view")),
    db: AsyncSession = Depends(get_db),
):
    """リソース一覧取得 - resource.view権限が必要"""
    ...
```

**変更内容**:
- ❌ 削除: `if current_user.role not in ["admin", "manager"]`
- ✅ 追加: `Depends(require_permission("resource.action"))`
- ✅ GET endpoints: 権限チェック追加
- ✅ POST/PUT/DELETE: ハードコードされたチェックを削除

**コード削減**:
- branches.py: 18行のハードコードを削除
- departments.py: 18行のハードコードを削除
- companies.py: 12行のハードコードを削除

---

### Pattern 2: 自己操作許可

**適用ルーター**: users.py, customers.py

**実装方法（users.py の例）**:
```python
from app.auth.permissions import require_any_permission, check_permission

@router.put("/{user_id}")
async def update_user(
    user_id: int,
    user_update: UserUpdate,
    current_user: User = Depends(require_any_permission(["user.update", "user.update_self"])),
    db: AsyncSession = Depends(get_db),
):
    """
    ユーザー更新

    必要な権限: user.update (他人も更新可能) OR user.update_self (自分のみ)
    """
    # 自分以外を更新する場合は user.update 権限が必要
    if user_id != current_user.id:
        has_update_permission = await check_permission(db, current_user.id, "user.update")
        if not has_update_permission:
            raise HTTPException(403, "他のユーザーを更新する権限がありません")
    ...
```

**特徴**:
- 自分自身の操作: `_self` 権限でOK
- 他人の操作: 完全な権限が必要
- 動的な権限確認: `check_permission()` 使用

**コード削減**:
- users.py: 18行のハードコードを削除
- customers.py: 追加変更なし（元々ロールチェックなし）

---

### Pattern 3: 動的スコープ

**適用ルーター**: daily_reports.py

**実装方法**:
```python
from app.auth.permissions import require_any_permission, check_permission

@router.get("")
async def get_daily_reports(
    user_id: int = None,
    current_user: User = Depends(require_any_permission(["report.view_all", "report.view_self"])),
    db: AsyncSession = Depends(get_db),
):
    """
    日報一覧取得

    必要な権限: report.view_all (全日報) OR report.view_self (自分の日報のみ)
    """
    query = select(DailyReport).where(DailyReport.company_id == current_user.company_id)

    # 権限に応じてスコープを制限
    has_view_all = await check_permission(db, current_user.id, "report.view_all")
    if not has_view_all:
        # view_selfのみの場合は自分の日報のみ
        query = query.where(DailyReport.user_id == current_user.id)
    elif user_id:
        # view_all権限があり、user_idが指定されている場合のみフィルタ
        query = query.where(DailyReport.user_id == user_id)

    ...
```

**特徴**:
- データスコープを権限に応じて動的に変更
- view_all: 全データにアクセス可能
- view_self: 自分のデータのみに自動的にフィルタリング
- 最も柔軟で安全なアクセス制御

**適用エンドポイント**:
- GET list: view_all/view_self の動的切り替え
- GET detail: view_all/view_self の動的チェック
- POST: create + 他人作成時はupdate必要
- PUT: update/update_self の動的チェック
- DELETE: delete/delete_self の動的チェック

**コード削減**:
- 24行のハードコードを削除
- より柔軟な権限制御を実現

---

## 📈 統計情報

### 削除されたコード

| ルーター | 削除行数 | 内容 |
|---------|---------|------|
| branches.py | 18行 | ハードコードされたロールチェック |
| departments.py | 18行 | ハードコードされたロールチェック |
| companies.py | 12行 | ハードコードされたロールチェック |
| users.py | 18行 | ハードコードされたロールチェック |
| daily_reports.py | 24行 | ハードコードされたロールチェック |
| **合計** | **90行** | - |

### 追加されたコード

| ルーター | 追加内容 |
|---------|---------|
| 全ルーター | import文の追加（require_permission, require_any_permission, check_permission） |
| 全ルーター | docstringへの権限要件の明記 |
| users.py | 動的権限チェックロジック（check_permission） |
| daily_reports.py | 動的スコープ制御ロジック（check_permission） |

---

## ✅ 実装された権限マッピング

### branches.py
- `GET /api/branches` → `branch.view`
- `GET /api/branches/{id}` → `branch.view`
- `POST /api/branches` → `branch.create`
- `PUT /api/branches/{id}` → `branch.update`
- `DELETE /api/branches/{id}` → `branch.delete`

### departments.py
- `GET /api/departments` → `department.view`
- `GET /api/departments/{id}` → `department.view`
- `POST /api/departments` → `department.create`
- `PUT /api/departments/{id}` → `department.update`
- `DELETE /api/departments/{id}` → `department.delete`

### companies.py
- `GET /api/companies` → `company.view`
- `GET /api/companies/{id}` → `company.view`
- `POST /api/companies` → `company.create` ✨新規
- `PUT /api/companies/{id}` → `company.update`
- `DELETE /api/companies/{id}` → `company.delete` ✨新規

### users.py
- `GET /api/users` → `user.view`
- `GET /api/users/{id}` → `user.view`
- `POST /api/users` → `user.create`
- `PUT /api/users/{id}` → `user.update` OR `user.update_self`
- `DELETE /api/users/{id}` → `user.delete`

### customers.py
- `GET /api/customers` → `customer.view`
- `GET /api/customers/{id}` → `customer.view`
- `POST /api/customers` → `customer.create`
- `PUT /api/customers/{id}` → `customer.update`
- `DELETE /api/customers/{id}` → `customer.delete`

### daily_reports.py
- `GET /api/daily-reports` → `report.view_all` OR `report.view_self`
- `GET /api/daily-reports/{id}` → `report.view_all` OR `report.view_self`
- `POST /api/daily-reports` → `report.create` (+ `report.update` for others)
- `PUT /api/daily-reports/{id}` → `report.update` OR `report.update_self`
- `DELETE /api/daily-reports/{id}` → `report.delete` OR `report.delete_self`

---

## 🔧 技術的な改善点

### 1. 宣言的な権限チェック

**Before**:
```python
async def create_branch(
    branch: BranchCreate,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """支店作成（管理者のみ）"""
    if current_user.role not in ["admin", "manager"]:
        raise HTTPException(403, "権限がありません")
    ...
```

**After**:
```python
async def create_branch(
    branch: BranchCreate,
    current_user: User = Depends(require_permission("branch.create")),
    db: AsyncSession = Depends(get_db),
):
    """
    支店作成

    必要な権限: branch.create
    """
    ...
```

### 2. 保守性の向上

- ✅ 権限変更時の修正箇所が1箇所（seed_permissions.py）に集約
- ✅ 各エンドポイントの必要権限が一目でわかる
- ✅ テストコードが書きやすい

### 3. 安全性の向上

- ✅ 企業レベルのアクセス制御は維持
- ✅ 権限チェックとビジネスロジックの分離
- ✅ 動的スコープ制御による柔軟なアクセス制御

---

## 📝 残タスク（将来の拡張）

### 1. customer.view_assigned の実装
**現状**: customer.view で全顧客を閲覧可能
**将来**: customer.view_assigned で担当顧客のみ閲覧可能にする

**実装例**:
```python
@router.get("")
async def get_customers(
    current_user: User = Depends(require_any_permission(["customer.view", "customer.view_assigned"])),
    db: AsyncSession = Depends(get_db),
):
    query = select(Customer).where(Customer.company_id == current_user.company_id)

    has_view_all = await check_permission(db, current_user.id, "customer.view")
    if not has_view_all:
        # view_assignedのみの場合は担当顧客のみ
        query = query.where(Customer.assigned_user_id == current_user.id)

    ...
```

### 2. report.approve と report.comment の実装
**現状**: エンドポイント未実装
**将来**: 日報承認機能とコメント機能を追加

**エンドポイント例**:
```python
@router.post("/{report_id}/approve")
async def approve_report(
    report_id: int,
    current_user: User = Depends(require_permission("report.approve")),
    db: AsyncSession = Depends(get_db),
):
    """日報承認 - report.approve権限が必要"""
    ...

@router.post("/{report_id}/comments")
async def add_comment(
    report_id: int,
    comment: CommentCreate,
    current_user: User = Depends(require_permission("report.comment")),
    db: AsyncSession = Depends(get_db),
):
    """コメント追加 - report.comment権限が必要"""
    ...
```

### 3. テストケースの追加

各ルーターに以下のテストケースを追加:
- ✅ 認証なしでアクセス → 401
- ✅ 権限なしでアクセス → 403
- ✅ 適切な権限でアクセス → 200/201
- ✅ 自分自身の操作 → 成功（_self権限）
- ✅ 他人の操作 → 失敗（_self権限のみの場合）

---

## 🎓 学んだこと

### 1. FastAPI Dependenciesの活用
- `Depends()` を使った宣言的な権限チェック
- 複数のDependencyを組み合わせた柔軟な制御

### 2. 権限システムの設計パターン
- **Pattern 1**: シンプルなリソースベース権限
- **Pattern 2**: 自己操作を許可する権限設計
- **Pattern 3**: 動的スコープ制御

### 3. セキュリティのベストプラクティス
- 権限チェックとビジネスロジックの分離
- 企業レベルのアクセス制御の維持
- 最小権限の原則の適用

---

## 📚 参考ドキュメント

- `claudedocs/権限管理.md` - 権限システムの設計仕様
- `claudedocs/権限割り当て戦略.md` - 実装戦略とパターン
- `claudedocs/branches_権限適用例.md` - 具体的な実装例
- `scripts/seed_permissions.py` - 権限とグループの定義

---

## 🚀 次のステップ

1. **PR作成**: feature/user-permission ブランチのプルリクエストを作成
2. **コードレビュー**: 実装内容の確認
3. **テスト実装**: 権限チェックのテストケースを追加
4. **マージ**: main ブランチへのマージ

---

**作成者**: Claude Code
**作成日**: 2026-01-03
**バージョン**: 1.0.0
